// Saved-job seed data. Job postings themselves live in admin.fixtures.ts
// (the same collection backs both the public job-finder listing and the
// admin job-postings CRUD table).

export const seedSavedJobs: any[] = [];
